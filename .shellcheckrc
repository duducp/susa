# ShellCheck configuration for Susa CLI project
# Documentation: https://github.com/koalaman/shellcheck/wiki/Configuration

# Disable specific checks globally

# SC1090: Can't follow non-constant source (we use dynamic sourcing)
disable=SC1090

# SC1091: Not following: file not found (external dependencies)
disable=SC1091

# SC2250: Prefer putting braces around variable references (too verbose for this project)
disable=SC2250

# SC2292: Prefer [[ ]] over [ ] for tests (we use [ ] intentionally for POSIX compatibility)
disable=SC2292

# SC2155: Declare and assign separately (common pattern in the project)
disable=SC2155

# SC2154: Variables from sourced files (color.sh exports color variables)
disable=SC2154

# SC2310: Functions invoked in || condition (intentional for error handling)
disable=SC2310

# SC2248: Prefer double quoting even when not needed (numeric comparisons)
disable=SC2248

# SC2249: Consider adding default case (not always necessary)
disable=SC2249

# SC2034: Variable appears unused (exported variables for external use)
disable=SC2034

# SC2312: Consider invoking separately (acceptable in this project)
disable=SC2312

# SC2181: Check exit code directly (sometimes clearer to check $?)
disable=SC2181

# SC2002: Useless cat (sometimes more readable)
disable=SC2002

# SC2178: Variable used as array but now string (nameref usage)
disable=SC2178

# SC2317: Command appears unreachable (indirect invocation)
disable=SC2317

# SC2153: Possible misspelling (false positives with exported vars)
disable=SC2153

# SC2086: Double quote to prevent globbing (intentional in some cases)
disable=SC2086

# SC2129: Use { cmd1; cmd2; } >> file (style preference)
disable=SC2129

# SC2230: which is non-standard (backward compatibility)
disable=SC2230

# SC2001: Use parameter expansion instead of sed (sometimes more readable)
disable=SC2001

# SC2162: read without -r (backward compatibility)
disable=SC2162

# Set default shell to bash
shell=bash

# Minimum severity level to report (error, warning, info, style)
severity=warning

# Enable optional checks
enable=all
